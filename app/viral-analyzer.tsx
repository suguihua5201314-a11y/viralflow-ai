"use client";
import {useState} from "react";
import type {AnalysisInput,ViralAnalysisResult,ViralCase} from "./viral-analysis";

type Props={cases:ViralCase[];onSave:(item:ViralCase)=>void;onReplicate:(item:ViralCase)=>void;product?:string;market?:string;language?:string};
const empty=(product="",market="",language=""):AnalysisInput=>({sourceText:"",inputType:"transcript",platform:"TikTok",market,language,product,category:"",durationSeconds:undefined,views:"",likes:"",comments:"",shares:"",saves:"",gmv:"",sourceUrl:"",notes:"",visualNotes:""});
export default function ViralAnalyzer({cases,onSave,onReplicate,product="",market="",language=""}:Props){
 const [input,setInput]=useState(()=>empty(product,market,language));const [result,setResult]=useState<ViralAnalysisResult|null>(null);const [busy,setBusy]=useState(false);const [error,setError]=useState("");
 const patch=(value:Partial<AnalysisInput>)=>{setInput(prev=>({...prev,...value}));setResult(null)};
 async function analyze(){setBusy(true);setError("");try{const res=await fetch("/api/analyze",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(input)});const data=await res.json();if(!res.ok)throw new Error(data.error||"分析失败");setResult(data.analysis);}catch(e){setError(e instanceof Error?e.message:"分析失败");}finally{setBusy(false)}}
 const currentCase=()=>result?{id:`viral-${Date.now()}`,title:`${input.product||input.category||"爆款内容"}｜${result.hook.type}`,createdAt:new Date().toISOString(),source:input,analysis:result}:null;
 return <section className="va-shell">
  <aside className="va-source"><span className="modal-kicker">SOURCE INPUT · OBSERVED</span><h2>输入爆款内容</h2><p>文案与Transcript是本阶段真实分析依据。没有提供的数据不会被补写。</p>
   <div className="va-toggle"><button className={input.inputType==="transcript"?"selected":""} onClick={()=>patch({inputType:"transcript"})}>Transcript</button><button className={input.inputType==="copy"?"selected":""} onClick={()=>patch({inputType:"copy"})}>文案</button></div>
   <label>原始文案 / Transcript<textarea rows={15} value={input.sourceText} onChange={e=>patch({sourceText:e.target.value})} placeholder="粘贴完整口播、字幕或竞品爆款文案…"/></label>
   <div className="va-fields"><label>平台<input value={input.platform} onChange={e=>patch({platform:e.target.value})}/></label><label>市场<input value={input.market} onChange={e=>patch({market:e.target.value})}/></label><label>语言<input value={input.language} onChange={e=>patch({language:e.target.value})}/></label><label>时长（秒）<input type="number" value={input.durationSeconds||""} onChange={e=>patch({durationSeconds:e.target.value?Number(e.target.value):undefined})}/></label><label>产品<input value={input.product} onChange={e=>patch({product:e.target.value})}/></label><label>品类<input value={input.category} onChange={e=>patch({category:e.target.value})}/></label></div>
   <details><summary>真实表现数据（可选）</summary><div className="va-fields"><label>播放<input value={input.views} onChange={e=>patch({views:e.target.value})}/></label><label>点赞<input value={input.likes} onChange={e=>patch({likes:e.target.value})}/></label><label>评论<input value={input.comments} onChange={e=>patch({comments:e.target.value})}/></label><label>分享<input value={input.shares} onChange={e=>patch({shares:e.target.value})}/></label><label>收藏<input value={input.saves} onChange={e=>patch({saves:e.target.value})}/></label><label>GMV / 销量<input value={input.gmv} onChange={e=>patch({gmv:e.target.value})}/></label></div></details>
   <label>用户补充画面描述（可选）<textarea rows={3} value={input.visualNotes} onChange={e=>patch({visualNotes:e.target.value})} placeholder="未提供时，Visual / Editing会明确标记unknown"/></label>
   {error&&<p className="va-error">{error}</p>}<button className="va-analyze" disabled={busy||input.sourceText.trim().length<20} onClick={analyze}>{busy?"DeepSeek 正在拆解…":"✦ 开始结构化拆解"}</button>
   <div className="va-legacy"><b>视频资产保留</b><span>历史上传与播放器仍在项目中；当前DeepSeek只分析Transcript，不声称看过视频画面。</span></div>
  </aside>
  <main className="va-analysis">{!result?<div className="va-empty"><span>◇</span><h2>等待内容分析</h2><p>结果将分离 Observed、AI Analysis 与 Replication Suggestion。</p>{cases.length>0&&<small>案例库已有 {cases.length} 条兼容案例</small>}</div>:<>
   <div className="va-head"><div><span>AI ANALYSIS · DEEPSEEK</span><h2>{result.summary}</h2></div><b>{result.score.total}<small>/100</small></b></div>
   <article className="va-hook"><span>HOOK DEEP DIVE</span><blockquote>{result.hook.original}</blockquote><div className="va-chips"><b>{result.hook.type}</b><b>{result.hook.mechanism}</b></div><p><strong>注意力机制</strong>{result.hook.whyItWorks}</p><p><strong>承诺 → 兑现</strong>{result.hook.promise} → {result.hook.payoff}</p></article>
   <section className="va-card"><h3>内容结构</h3>{result.structure.map((x,i)=><article className="va-stage" key={`${x.stage}-${i}`}><b>{i+1}</b><div><strong>{x.stage}</strong><p>{x.content}</p><small>{x.purpose} · {x.viewerPsychology}</small></div></article>)}</section>
   <section className="va-card"><h3>Visual / Voice / Editing 时间线</h3><div className="va-timeline-meta"><span>{result.metadata.timelineEstimated?"ESTIMATED TIMELINE":"PROVIDED TIMELINE"}</span><span>{result.metadata.visualEvidenceAvailable?"有用户画面证据":"无画面证据"}</span></div>{result.timeline.map((x,i)=><article className="va-time" key={`${x.start}-${i}`}><b>{x.start}–{x.end}</b><div><strong>{x.stage}</strong><p><em>Voice</em>{x.spokenContent}</p><p><em>Visual</em>{x.visualDescription}</p><p><em>Editing</em>{x.editingNote}</p><small>{x.retentionMechanism}</small></div></article>)}</section>
   <div className="va-two"><section className="va-card"><h3>Selling Point / Proof</h3>{result.sellingPoints.map((x,i)=><p key={i}><b>{x.priority}</b> {x.point}<small>Observed evidence: {x.evidence}</small></p>)}{result.proofMechanisms.map((x,i)=><p key={`p${i}`}><b>{x.observed?"OBSERVED":"AI ANALYSIS"}</b> {x.type} · {x.content}</p>)}</section><section className="va-card"><h3>Retention / Emotion</h3>{result.pacing.retentionBeats.map((x,i)=><p key={i}>↳ {x}</p>)}{result.emotionCurve.map((x,i)=><p key={`e${i}`}><b>{x.emotion}</b> {x.stage} · {x.trigger}</p>)}</section></div>
  </>}</main>
  <aside className="va-intel">{!result?<><span className="modal-kicker">VIRAL INTELLIGENCE</span><h2>案例库</h2>{cases.slice(0,5).map(item=><article className="va-saved" key={item.id}><b>{item.title}</b><small>{new Date(item.createdAt).toLocaleString("zh-CN")}</small><button onClick={()=>onReplicate(item)}>基于此案例复刻</button></article>)}</>:<>
   <span className="modal-kicker">VIRAL INTELLIGENCE</span><h2>为什么可能有效</h2><section className="va-card">{result.viralMechanisms.map((x,i)=><p key={i}>{i+1}. {x}</p>)}</section>
   <h3>ViralFlow 内容分析评分</h3><section className="va-scores">{Object.entries(result.score).filter(([,v])=>typeof v==="number").map(([k,v])=><div key={k}><span>{k}</span><b>{v as number}</b></div>)}<small>规则评分，不是播放量、爆款概率或留存预测。</small></section>
   <h3>可复刻公式</h3><section className="va-formula">{result.reusableFormula.map((x,i)=><div key={i}><b>{i+1}</b><span>{x}</span></div>)}</section>
   <h3>Replication Suggestion</h3><section className="va-card"><b>VISUAL</b>{result.replicationNotes.visualSuggestions.map((x,i)=><p key={i}>{x}</p>)}<b>COPY</b>{result.replicationNotes.copySuggestions.map((x,i)=><p key={i}>{x}</p>)}<b>EDITING</b>{result.replicationNotes.editingSuggestions.map((x,i)=><p key={i}>{x}</p>)}</section>
   {result.risks.length>0&&<section className="va-risks"><b>风险</b>{result.risks.map((x,i)=><p key={i}>! {x}</p>)}</section>}
   <div className="va-actions"><button disabled={!result} onClick={()=>{const item=currentCase();if(item)onSave(item)}}>保存到爆款案例库</button><button disabled={!result} onClick={()=>{const item=currentCase();if(item)onReplicate(item)}}>基于此案例复刻 →</button></div>
  </>}</aside>
 </section>;
}
