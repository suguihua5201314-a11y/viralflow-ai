"use client";
import {useEffect,useRef,useState} from "react";
type Timeline={start:number;end:number;stage:string;purpose:string;voiceover:string;visual:string;editing:string};
type Report={title:string;score:number;summary:string;hook:string;hookType:string;pacing:string;timeline:Timeline[];shotRhythm:string[];sellingPoints:string[];proof:string[];cta:string;risks:{level:string;issue:string;advice:string}[];formula:string;remakeScript:string};
const STORE="susu-video-analysis-v1",steps=["读取视频","提取音频和关键帧","识别口播","分析结构","生成报告"];
async function captureFrames(v:HTMLVideoElement){const c=document.createElement("canvas"),scale=Math.min(1,384/v.videoWidth);c.width=Math.round(v.videoWidth*scale);c.height=Math.round(v.videoHeight*scale);const x=c.getContext("2d")!,out=[];for(let i=0;i<6;i++){const time=Math.min(v.duration-.05,v.duration*i/5);await new Promise<void>((ok,bad)=>{v.onseeked=()=>ok();v.onerror=()=>bad();v.currentTime=time});x.drawImage(v,0,0,c.width,c.height);out.push({time:+time.toFixed(1),image:c.toDataURL("image/jpeg",.42)})}return out}
async function extractAudio(file:File){const ctx=new AudioContext();try{const source=await ctx.decodeAudioData(await file.arrayBuffer()),rate=16000,length=Math.ceil(source.duration*rate),offline=new OfflineAudioContext(1,length,rate),node=offline.createBufferSource();node.buffer=source;node.connect(offline.destination);node.start();const rendered=await offline.startRendering(),samples=rendered.getChannelData(0),buffer=new ArrayBuffer(44+samples.length*2),view=new DataView(buffer),write=(o:number,s:string)=>{for(let i=0;i<s.length;i++)view.setUint8(o+i,s.charCodeAt(i))};write(0,"RIFF");view.setUint32(4,36+samples.length*2,true);write(8,"WAVEfmt ");view.setUint32(16,16,true);view.setUint16(20,1,true);view.setUint16(22,1,true);view.setUint32(24,rate,true);view.setUint32(28,rate*2,true);view.setUint16(32,2,true);view.setUint16(34,16,true);write(36,"data");view.setUint32(40,samples.length*2,true);for(let i=0;i<samples.length;i++){const n=Math.max(-1,Math.min(1,samples[i]));view.setInt16(44+i*2,n<0?n*32768:n*32767,true)}return new Blob([buffer],{type:"audio/wav"})}finally{await ctx.close()}}
async function readJson(r:Response){const raw=await r.text();try{return JSON.parse(raw)}catch{if(r.status===413||/payload too large/i.test(raw))throw Error("上传数据过大，请刷新页面后使用新版重试。");throw Error(`接口返回异常（${r.status}），请稍后重试。`)}}
export default function VideoAnalyzer({products}:{products:string[]}){const input=useRef<HTMLInputElement>(null),video=useRef<HTMLVideoElement>(null);const[file,setFile]=useState<File|null>(null),[url,setUrl]=useState(""),[duration,setDuration]=useState(0),[product,setProduct]=useState(products[0]||""),[busy,setBusy]=useState(false),[step,setStep]=useState(0),[error,setError]=useState(""),[report,setReport]=useState<Report|null>(null),[saved,setSaved]=useState<Report[]>([]);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem(STORE)||"[]"))}catch{}},[]);useEffect(()=>()=>{if(url)URL.revokeObjectURL(url)},[url]);function choose(f?:File){if(!f)return;setError("");setReport(null);if(f.size>500*1024*1024){setError("视频超过500MB，请压缩后重试。");return}setFile(f);setUrl(URL.createObjectURL(f))}async function run(){if(!file||!video.current)return;setBusy(true);setError("");try{if(!duration||duration>600)throw Error("请上传10分钟以内的视频");setStep(1);const frameData=await captureFrames(video.current);let audio:Blob;try{audio=await extractAudio(file)}catch{throw Error("无法读取视频音轨，请转换为常见MP4（H.264＋AAC）后重试。")}setStep(2);const fd=new FormData();fd.append("file",audio,"audio.wav");const tr=await fetch("/api/video/transcribe",{method:"POST",body:fd}),tj=await readJson(tr);if(!tr.ok)throw Error(tj.error||"口播识别失败");setStep(3);const ar=await fetch("/api/video/analyze",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({transcript:tj.text,frames:frameData,duration,product,fileName:file.name})}),aj=await readJson(ar);if(!ar.ok)throw Error(aj.error||"视频分析失败");setStep(4);setReport(aj.report)}catch(e){setError(e instanceof Error?e.message:"分析失败")}finally{setBusy(false)}}function save(){if(!report)return;const next=[report,...saved].slice(0,30);setSaved(next);localStorage.setItem(STORE,JSON.stringify(next))}return <section className="video-analyzer"><div className="video-upload-card"><span className="modal-kicker">AI VIDEO DECONSTRUCTOR</span><h2>上传视频，自动拆解爆款框架</h2><p>视频保留在本机，只上传压缩音频和关键画面。</p><div className="video-drop" onClick={()=>input.current?.click()} onDragOver={e=>e.preventDefault()} onDrop={e=>{e.preventDefault();choose(e.dataTransfer.files[0])}}><input ref={input} hidden type="file" accept="video/*" onChange={e=>choose(e.target.files?.[0])}/><b>＋</b><strong>{file?file.name:"点击或拖入视频"}</strong><small>{file?`${(file.size/1048576).toFixed(1)} MB · ${duration.toFixed(1)}秒`:"MP4 / WebM / MOV · ≤500MB · ≤10分钟"}</small></div>{url&&<video ref={video} src={url} controls preload="metadata" onLoadedMetadata={e=>setDuration(e.currentTarget.duration)}/>}<label>关联产品<select value={product} onChange={e=>setProduct(e.target.value)}>{products.map(x=><option key={x}>{x}</option>)}<option value="">不关联</option></select></label>{busy&&<div className="video-progress">{steps.map((x,i)=><span className={i<step?"done":i===step?"active":""} key={x}><i>{i<step?"✓":i+1}</i>{x}</span>)}</div>}{error&&<p className="video-error">{error}</p>}<button className="video-start" disabled={!file||busy} onClick={run}>{busy?`${steps[step]}中…`:"开始完整拆解"}</button></div><div className="video-report">{!report?<div className="video-empty"><b>▶</b><h3>等待视频</h3><p>结果包含前3秒钩子、逐秒框架、镜头、口播、卖点、证明、CTA与风险分级。</p>{saved.length>0&&<small>本机已保存 {saved.length} 份报告</small>}</div>:<><header><div><span>综合爆款评分</span><h2>{report.title}</h2><p>{report.summary}</p></div><strong>{report.score}<small>/100</small></strong></header><div className="video-kpis"><article><span>前3秒钩子</span><b>{report.hookType}</b><p>{report.hook}</p></article><article><span>节奏</span><b>{report.pacing}</b><p>{report.shotRhythm.join(" · ")}</p></article><article><span>转化收口</span><b>CTA</b><p>{report.cta}</p></article></div><section className="video-timeline"><h3>逐秒结构时间轴</h3>{report.timeline.map((x,i)=><article key={i}><time>{x.start}s–{x.end}s</time><div><b>{x.stage}</b><p>{x.purpose}</p><small>画面：{x.visual}</small><small>口播：{x.voiceover}</small><em>{x.editing}</em></div></article>)}</section><div className="video-two"><section><h3>卖点与证明</h3>{report.sellingPoints.map(x=><p key={x}>✓ {x}</p>)}{report.proof.map(x=><p key={x}>◇ {x}</p>)}</section><section><h3>违规与拍摄风险</h3>{report.risks.length?report.risks.map((x,i)=><p key={i} className={`risk-${x.level}`}>[{x.level}] {x.issue} — {x.advice}</p>):<p>✓ 暂未发现明显风险</p>}</section></div><article className="video-formula"><span>可复刻公式</span><strong>{report.formula}</strong></article><article className="video-remake"><span>一键复刻脚本</span><p>{report.remakeScript}</p></article><button className="video-save" onClick={save}>保存本次报告</button></>}</div></section>}
